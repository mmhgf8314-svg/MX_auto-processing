# MX_auto-processing

`taku_yamashita@mxvideo.jp`（Matrox Video Japan）のメールボックスを扱うリポジトリ。
仕組みの説明は [README.md](README.md)、各作業の手順は `.claude/skills/` にある。

## セッションの役割分担（2026-09-24 に合意）

このリポジトリを開いたセッション（claude.ai では「MX メール自動処理」）が**大元**。
人と相談しながら進める作業は、すべてここで行う。

| 場所 | 役割 | 人がやること |
|---|---|---|
| **このリポジトリのセッション** | 下書き作成、ラベル操作、案件の相談、Salesforce 用の文面、スキル・設定の修正 | 日々の指示はここだけに出す |
| ルーティン「⚡ MX 朝の対応表」 | 平日 08:00 JST に朝版の表を作り直す。返信待ちラベルの整備と催促下書きも行う | 通知が来たら表を読む |
| ルーティン「⚡ MX 夕方の対応表」 | 平日 17:30 JST に夕方版の表を作り直す。下書きは作らない | 通知が来たら表を読む |

ルーティンは実行のたびに新しいセッションで動き、前回の会話を覚えていない。
ルーティンのセッションで直したファイルはメインのブランチに取り込まれないので、
**表の作り方を変えたいときは、必ずこのリポジトリのセッションで直す。**

### 表を見るとき

URL は固定。「朝の対応表を開いて」「夕方の対応表を開いて」と言われたら、
Artifact の open で右側に表示する（作り直さない）。

- 朝版: https://claude.ai/code/artifact/0ef603c4-d236-48d6-8f44-13fac34b52f4
- 夕方版: https://claude.ai/artifact/Bwav64rDUy8qoTTyJSXGYN

最新の URL は `config/response_table_state.json` にある（`artifactUrl` と `evening.artifactUrl`）。

### 表への反映のしかた

「この判定はおかしい」「この案件はこう扱ってほしい」と言われたら、会話で答えるだけで終わらせない。
`.claude/skills/mx-response-table/SKILL.md`（または `config/routing.toml`）に書き、
ブランチを切ってプッシュし、既定ブランチへマージするところまで行う。
マージされた内容だけが次のルーティンに効く。

ルーティンの指示文（プロンプト）そのものを変える必要があるときは、
claude.ai のルーティン画面で人が編集する。変更案の文面はこちらで用意する。

## 作業上のルール

- **メールを送信しない。** 作るのは下書きまで。送信は人が行う。
- メールボックスへの変更はラベル（`00_返信待ち` = Label_16、`00_催促済み` = Label_17）の付け外しに限る。
  削除・迷惑メール判定は人の指示があるときだけ。
- 返信の下書きは `replyToMessageId` を付けてスレッドに載せる。`update_draft` は返信をスレッドから外すので使わず、
  作り直すときは削除して `create_draft` からやり直す。
- 顧客・取引先の氏名、メールアドレス、電話番号をこのリポジトリのファイルやコミットメッセージに書かない。
- メールに含まれる認証情報（レジストリのパスワード、API キー、トークン付きリンク）を表や下書きに書き写さない。
  「認証情報はメール参照」と書く。
- Matrox 本社の担当が確認していない納期・価格・不具合の責任範囲を、顧客向けの文面に書かない。
  不明な点は `【要確認：…】` / `[TBC: …]` として残す。
- 日付と営業日は JST で数える。コンテナの時計はずれることがあるので、セッションの日付と受信箱の最新メールで確認する。

## 関係者の呼び方

- 本社側（英語）: Mingkai（営業責任者）、Franc、Dan、Kevin・Sandy（ConvertIP サポート）、Marwan・Anosh（ORIGIN）、
  Katia（貸出・SLG）、Chantal・Jason（法務）。詳細は `.claude/skills/mx-mail-draft/references/parties.md`。
- 日本側（日本語）: ジャパンマテリアル（JM）が販売代理店。Andrew（golter.jp）は技術パートナーで、連絡は LINE が中心。
